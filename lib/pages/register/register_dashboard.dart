import 'package:flutter/material.dart';

class RegDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Home Dashboard'),
      ),
      body: Center(
        child: Text('Welcome to the Home Dashboard!'),
      ),
    );
  }
}
